import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.movielist',
  appName: 'movielist',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
